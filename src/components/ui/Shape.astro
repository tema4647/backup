---
const { class: className, ...rest } = Astro.props;

---

<div class:list={["shape", className]} {...rest}><slot /></div>

<style is:global>
  .shape {
    width: 100%;
    min-height: 300px;
    height: auto;
    background-color: var(--grey-color);
    border-radius: var(--border-radius);
    padding: 50px;
    overflow: hidden;
  }
  
</style>
